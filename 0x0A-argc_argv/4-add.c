#include <stdio.h>
#include <stdlib.h>

/**
 * main - it adds all positive numbers
 * @argc: int
 * @argv: array of strings
 * Return: 0
 */

int main(int argc, char **argv)
{
	if (argc == 1)
	{
		printf("%d\n", 0);
	}else if ()
}
